// Offline fallback only. There is NO predefined demo data: a real workspace
// is loaded from the backend - WorkDrive users are the live HRMS employees,
// and folders/files/shares are only what the team actually creates.
export const users = [];
export const currentUserId = 'me';
export const teamFolders = [];
export const items = [];
export const seedShares = {};
